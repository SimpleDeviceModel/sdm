#ifndef SDMDIRSCFG_H_INCLUDED
#define SDMDIRSCFG_H_INCLUDED

#define INSTALL_PREFIX_FALLBACK "${CMAKE_INSTALL_PREFIX}"
#define BIN_INSTALL_DIR "${BIN_INSTALL_DIR}"
#define PLUGINS_INSTALL_DIR "${PLUGINS_INSTALL_DIR}"
#define LIB_INSTALL_DIR "${LIB_INSTALL_DIR}"
#define INCLUDE_INSTALL_DIR "${INCLUDE_INSTALL_DIR}"
#define LUA_MODULES_INSTALL_DIR "${LUA_MODULES_INSTALL_DIR}"
#define LUA_CMODULES_INSTALL_DIR "${LUA_CMODULES_INSTALL_DIR}"
#define QT_INSTALL_DIR "${QT_INSTALL_DIR}"
#define DOC_INSTALL_DIR "${DOC_INSTALL_DIR}"
#define I18N_INSTALL_DIR "${I18N_INSTALL_DIR}"
#define SCRIPTS_INSTALL_DIR "${SCRIPTS_INSTALL_DIR}"
#define DATA_INSTALL_DIR "${DATA_INSTALL_DIR}"

#endif
