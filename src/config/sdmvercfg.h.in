#ifndef SDMVERCFG_H_INCLUDED
#define SDMVERCFG_H_INCLUDED

#define SDM_VERSION "${PRODUCT_VERSION_MAJOR}.${PRODUCT_VERSION_MINOR}.${PRODUCT_VERSION_PATCH}${PRODUCT_VERSION_SUFFIX}"
#define SDM_SHORTVERSION "${PRODUCT_VERSION_MAJOR}.${PRODUCT_VERSION_MINOR}.${PRODUCT_VERSION_PATCH}"
#define SDM_COMMIT_HASH "${GIT_COMMIT_HASH}"
#define SDM_COMMIT_TIMESTAMP ${COMMIT_TIMESTAMP}

#define SDM_COMPILER "${COMPILER_ID} ${COMPILER_VERSION}"

#define SDM_OS "${SYSTEM_NAME}"
#define SDM_ARCH "${TARGET_MACHINE}"

#endif
